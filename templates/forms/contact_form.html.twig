<div class="section section-white">
    <div class="container">
        <div class="section-inner d-flex">
            <div class="col-lg-5 col-md-4 d-none d-md-block">
                <div class="image-container col-lg-10 col-md-12">
                    <img src="{{ asset('img/cat-contact.webp') }}" alt="cat">
                </div>
            </div>

            <div class="col-lg-6 col-md-7">
                <div class="title ungapped">
                    <h2>Contact us</h2>
                </div>
                <p class="form-desc col-10">If you have any questions or cooperation issues, please sign up to have request to the extra conent!</p>
                <div class="col-lg-7 col-md-10 form-wrapper">
                {{ form_start(form, {'attr': {'id': 'contact-form'}}) }}
                    {{ form_row(form.name) }}
                    {{ form_row(form.email) }}
                    {{ form_row(form.phone, {'attr': {'id': 'phone'}}) }}
                    {{ form_row(form.message) }}
                    <button class="btn btn-primary">Send request</button>
                    {{ form_end(form) }}

                </div>
            </div>
        </div>
    </div>
</div>
